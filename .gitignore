data/
logs/
rust/target/
node_modules/
ui-shots/
hub.env
# Hà để khoá Telegram ở đây 2026-08-10 và nó CHƯA từng được bỏ qua — một dòng
# `git add -A` là token vào repo. Bịt trước, rồi mới bàn chuyện đọc nó ở đâu.
.env
hub.config.json.bak*
*.sqlite
*.sqlite-shm
*.sqlite-wal
.DS_Store
.tmp/

# Không lên repo công khai — thêm 2026-08-13 khi mở repo public:
# ảnh Hà gửi qua Telegram, log của daemon, và file quy định phạm vi daemon
# ("this file is the security boundary" — chính nó tự khai).
.inbox/
scripts/.cmd-results/
.runner-allowlist
.runner-allowlist.bak*
